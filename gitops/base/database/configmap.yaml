apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    app: postgres
    component: database
    shop: bugatti
data:
  database.name: "bugatti_shop"
  database.port: "5432"

# Made with Bob
